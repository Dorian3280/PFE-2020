import styled from 'styled-components';

const Grid = styled.div`
    position: relative;
    z-index: 3;
    height: 100vh;
    display: grid;
    grid-template-columns: 20% 60% 20%;
`;

export default Grid;